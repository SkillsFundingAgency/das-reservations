@model ApprenticeshipTrainingViewModel
﻿
<label class="govuk-label govuk-label--m" for="course-search">Which apprenticeship training will the apprentice take? (optional)</label>
<select id="course-search" class="govuk-select">
    @foreach (var course in Model.Courses)
    {
        <option>@course.Title - Level: @course.Level</option>
    }
</select>